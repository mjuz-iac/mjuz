{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "yarn workspaces foreach -p run build",
    "clean": "yarn workspaces foreach -p run clean",
    "install-protos": "yarn workspace @mjus/grpc-protos prepublish && rm -r node_modules/@mjus/grpc-protos && cp -r mjus-grpc-protos node_modules/@mjus/grpc-protos",
    "postinstall": "patch-package && yarn install-protos"
  },
  "devDependencies": {
    "patch-package": "^6.2.2"
  },
  "workspaces": [
    "mjus-core",
    "mjus-grpc-protos",
    "teastore",
    "webpage"
  ]
}
